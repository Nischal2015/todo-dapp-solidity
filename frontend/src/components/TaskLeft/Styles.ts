import styled from "@emotion/styled";

export const StyledTaskLeft = styled.div({
  height: "2rem",
  width: "2rem",
  borderRadius: "50%",
  boxShadow: "inset 0 0 0 2.5px #cdcddc",
  backgroundColor: "#fff",
  cursor: "pointer",
});
