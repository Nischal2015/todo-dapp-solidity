import styled from "@emotion/styled";

export const StyledContainer = styled.section({
  maxWidth: "80rem",
  margin: "0 auto",
  padding: "0 2.4rem",
  background: "linear-gradient(#f4f5ff 2%, #fdfdff 20%)",
});
